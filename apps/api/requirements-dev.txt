-r requirements.txt

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# Linting / formatting / typing
ruff>=0.7.0
mypy>=1.11.0

# Security analysis (the security workflow also installs these into its own env)
bandit>=1.7.10
pip-audit>=2.7.0

# Keep this list identical to [project.optional-dependencies].dev in
# pyproject.toml. CI installs *this* file, so a tool listed only there is
# invisible to CI: `mypy` was missing here, and the API quality job failed with
# "No module named mypy" on every run.
